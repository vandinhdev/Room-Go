server:
  port: 8761   # Port mặc định của Eureka Server

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false   # Server không tự đăng ký chính nó
    fetch-registry: false         # Không cần lấy registry từ nơi khác
